"""Driver Model (M6): deterministic, versioned per-fundamental scoring.

Reads the permanent compact rows M1-M5 already persist; nothing upstream is
rewritten. See docs/ARCHITECTURE_VISION.md (the Scoring Contract) and
docs/SPEC.md, Milestone 6.
"""
