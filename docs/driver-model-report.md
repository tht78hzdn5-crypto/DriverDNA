# Driver Model report — deterministic per-fundamental scoring (M6)

Generated by `driverdna model`. Scoring model `dm-v2`, taxonomy `pyramid-v1`. Every score ships with its confidence and evidence count — a model estimate, not a measurement of truth; more laps (more sessions, tracks, cars) sharpen it. `no_signal` fundamentals never score, at any confidence.

## owner

| fundamental | signal | score | confidence | evidence (laps) | trend | note |
|---|---|---|---|---|---|---|
| braking | measured | 80.5 | 60% | 12 | unavailable |  |
| commitment | proxy | 56.1 | 50% | 12 | unavailable | Scored on consistency and opportunity alone. This fundamental has no detectors of its own, so adherence can never be measured for it. |
| consistency | measured | 34.3 | 60% | 12 | unavailable | Scored on consistency alone. This fundamental has no detectors and no phase windows of its own, so adherence and opportunity can never be measured for it. |
| corner_exit | measured | 65.2 | 60% | 12 | unavailable |  |
| rotation | measured | 60.2 | 60% | 12 | unavailable |  |
| vehicle_management | proxy | 0.0 | 50% | 12 | unavailable | Scored on consistency alone. This fundamental has no detectors and no phase windows of its own, so adherence and opportunity can never be measured for it. Only 1 of its 4 techniques carries a telemetry signal — slip_management, tire_utilization and weight_transfer cannot be observed. |
| vision | no_signal | — | 0% | 0 | unavailable | no telemetry channel for this fundamental — never inferred (docs/COACHING.md's tri-state signal rule) |

### owner — strongest and weakest (`read-v1`)

- **Strongest: braking** — 80.52 (confidence 0.60, 12 laps)
- **Weakest: consistency** — 34.31 (confidence 0.60, 12 laps)

46.21 points apart. Ranked within this driver only — never against other drivers and never against an absolute standard, because these scores are not calibrated to any driver population. Only measured fundamentals take a verdict slot; 2 proxy fundamental(s) are ranked below but never named, since naming a greatest weakness from an indirect inference would headline the least-supported number here.

### owner — score history (`dm-hist-v1`)

Unavailable: too few dated laps to fill `model.history_buckets` (6) buckets at `model.trend_min_laps_per_bucket` (4) laps each — stated, not guessed.

