// The seven fundamentals, in the Driver Model pyramid's fixed order —
// foundations first, higher-order skills after. Every surface that lists or
// draws fundamentals reads this one array, so the cohort feedback section,
// the corner drill and #/model can never present them in two different
// orders.
//
// Layout only: fixed regardless of score, so the order never implies a
// ranking. The *labels* are deliberately not here — they come from the
// payload (`belief.label`), the engine owning its own words (A46).
export const FUNDAMENTAL_ORDER = [
  "braking", "rotation", "corner_exit",
  "commitment", "consistency", "vehicle_management", "vision",
];
